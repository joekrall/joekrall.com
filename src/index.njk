---
layout: base
title: Blog
tags: page
---

<div class="post-list">
{% for post in collections.posts | reverse %}
{% if not post.data.tags.includes('RSS Only') %}
  <div class="post">
    <h2>
      <a href="{{ post.url }}">{{ post.data.title }}</a>
    </h2>
    <time>{{ post.data.page.date | postDate }}</time>
  </div>
{% endif %}
{% endfor %}
</div>