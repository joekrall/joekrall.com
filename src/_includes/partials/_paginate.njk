{% set previousPost = collections.posts | noRssOnlyPosts | getPreviousCollectionItem(page) %}
{% set nextPost = collections.posts | noRssOnlyPosts | getNextCollectionItem(page) %}
<br>
<nav class="post">
  {% if previousPost %}
  <span>Previous: <a href="{{ previousPost.url }}">{{ previousPost.data.title }}</a></span>
  {% else %}
  <br>
  {% endif %}
  {% if nextPost %}
  <span>Next: <a href="{{ nextPost.url }}">{{ nextPost.data.title }}</a></span>
  {% else %}
  <br>
  {% endif %}
</nav>