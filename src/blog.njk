---
layout: base
title: Blog
tags: page
---

{% for post in collections.post | reverse %}
    <div class="blog-post-flexi-wrap">
        <a href="{{ post.url }}">{{ post.data.title }}</a>
        <time>{{ post.data.page.date.toLocaleDateString('en-US') }}</time>
    </div>
{% endfor %}